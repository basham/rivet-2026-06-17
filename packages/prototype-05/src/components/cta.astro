---
import { getUrl } from "../util.js";
---

<div class="rvt-button-group">
	<a
		class="rvt-button rvt-button--cta rvt-button--full-width"
		href={getUrl("/volunteer")}
	>
		Volunteer at the zoo
	</a>
	<a
		class="rvt-button rvt-button--cta rvt-button--full-width"
		href={getUrl("/membership")}
	>
		Become a member
	</a>
</div>
